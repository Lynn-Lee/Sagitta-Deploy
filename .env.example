# Sagitta Control 客户部署环境变量。
# 启动前复制为 .env，并替换所有 CHANGE_ME 值。

POSTGRES_DB=sagitta_control
POSTGRES_USER=sagitta
POSTGRES_PASSWORD=CHANGE_ME_STRONG_POSTGRES_PASSWORD
REDIS_PASSWORD=CHANGE_ME_STRONG_REDIS_PASSWORD

# 生产环境必须使用稳定的 32 位以上随机密钥。
# 数据加密后不要再修改 SECRET_KEY。
SECRET_KEY=CHANGE_ME_RANDOM_32_PLUS_CHARS

# 公网生产环境必须通过 HTTPS 发送浏览器登录 Cookie，不允许开启 HTTP 源码测试豁免。
AUTH_COOKIE_SECURE=true
ALLOW_INSECURE_AUTH_COOKIE=false
AUTH_COOKIE_SAMESITE=strict
AUTH_COOKIE_DOMAIN=

# Sagitta Control 授权校验。
# LICENSE_PUBLIC_KEY 是官方 License-Server-Center 的验签公钥，不是密钥。
# 在线激活和联网刷新会自动提交授权项目码 sagitta-control。
# 仅在使用私有授权中心或公钥轮换时替换。
LICENSE_PUBLIC_KEY=3Jz3SK-mTWZwGy6VX8gUBUWJ-kisvGnO3c_x18Fk_Ms
LICENSE_CUSTOMER_ID=
# 内网部署请把 LICENSE_SERVER_URL 留空：在线激活与联网刷新本就不通，
# 留空后系统按离线模式放宽本地试用窗口（7 天 -> 30 天），便于完成离线授权导入。
LICENSE_SERVER_URL=https://license.loveai.asia
LICENSE_AUTO_REFRESH_ENABLED=true
LICENSE_ONLINE_GRACE_DAYS=7
LICENSE_RENEWAL_NOTIFY_DAYS=30,21,14,7,1
# 商业 License 部署指纹绑定使用的稳定部署 ID。
# 首次部署生成一次，升级时保持不变，不要在日志中公开。
LICENSE_DEPLOYMENT_ID=CHANGE_ME_STABLE_RANDOM_DEPLOYMENT_ID
# 离线 Challenge 有效期；内网需把 Challenge 带出网签发再带回，默认给一天。
LICENSE_OFFLINE_CHALLENGE_TTL_MINUTES=1440
LICENSE_ALLOW_LEGACY_LICENSE_IMPORT=false

# 商业镜像完整性校验。MANIFEST_PUBLIC_KEY 是 Manifest 验签公钥，不是密钥。
APP_INTEGRITY_REQUIRED=true
APP_INTEGRITY_MANIFEST=/app/COMMERCIAL-MANIFEST.json
APP_INTEGRITY_ROOT=/app
MANIFEST_PUBLIC_KEY=gh279p2WaFR1_yekxXjB7gm8hJWxwWES7hb9aYRLJdU

APP_ENV=production
DEBUG=false
LOG_LEVEL=WARNING

BACKEND_PORT=8000
FRONTEND_PORT=80
CELERY_CONCURRENCY=8

ENABLE_GOINCEPTION=false
GO_INCEPTION_HOST=goinception:4000

ORACLE_DRIVER_MODE=auto
ORACLE_CLIENT_LIB_DIR=
ORACLE_CLIENT_CONFIG_DIR=
