apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "sagittadb.fullname" . }}-worker
  labels:
    {{- include "sagittadb.labels" . | nindent 4 }}
    app.kubernetes.io/component: worker
spec:
  replicas: {{ .Values.celeryWorker.replicaCount }}
  selector:
    matchLabels:
      {{- include "sagittadb.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: worker
  template:
    metadata:
      labels:
        {{- include "sagittadb.selectorLabels" . | nindent 8 }}
        app.kubernetes.io/component: worker
    spec:
      serviceAccountName: {{ include "sagittadb.fullname" . }}
      securityContext:
        {{- toYaml .Values.podSecurityContext | nindent 8 }}
      containers:
        - name: worker
          image: "{{ .Values.image.registry }}/{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          command:
            - celery
            - -A
            - app.celery_app
            - worker
            - --concurrency={{ .Values.celeryWorker.concurrency }}
            - -Q
            - {{ .Values.celeryWorker.queues }}
            - --loglevel=info
          envFrom:
            - configMapRef:
                name: {{ include "sagittadb.fullname" . }}
            - secretRef:
                name: {{ include "sagittadb.fullname" . }}
          resources:
            {{- toYaml .Values.celeryWorker.resources | nindent 12 }}
          securityContext:
            {{- toYaml .Values.securityContext | nindent 12 }}
          volumeMounts:
            - name: tmp
              mountPath: /tmp
          {{ if .Values.persistence.enabled }}
            - name: downloads
              mountPath: /app/downloads
          {{ end }}
      volumes:
        - name: tmp
          emptyDir: {}
      {{ if .Values.persistence.enabled }}
        - name: downloads
          persistentVolumeClaim:
            claimName: {{ include "sagittadb.fullname" . }}-downloads
      {{ end }}
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "sagittadb.fullname" . }}-beat
  labels:
    {{- include "sagittadb.labels" . | nindent 4 }}
    app.kubernetes.io/component: beat
spec:
  replicas: 1  # Beat 必须保持单实例运行
  selector:
    matchLabels:
      {{- include "sagittadb.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: beat
  template:
    metadata:
      labels:
        {{- include "sagittadb.selectorLabels" . | nindent 8 }}
        app.kubernetes.io/component: beat
    spec:
      serviceAccountName: {{ include "sagittadb.fullname" . }}
      securityContext:
        {{- toYaml .Values.podSecurityContext | nindent 8 }}
      containers:
        - name: beat
          image: "{{ .Values.image.registry }}/{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          command: ["celery", "-A", "app.celery_app", "beat", "--loglevel=info", "--schedule=/tmp/celerybeat-schedule"]
          envFrom:
            - configMapRef:
                name: {{ include "sagittadb.fullname" . }}
            - secretRef:
                name: {{ include "sagittadb.fullname" . }}
          resources:
            {{- toYaml .Values.celeryBeat.resources | nindent 12 }}
          securityContext:
            {{- toYaml .Values.securityContext | nindent 12 }}
          volumeMounts:
            - name: tmp
              mountPath: /tmp
      volumes:
        - name: tmp
          emptyDir: {}
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
